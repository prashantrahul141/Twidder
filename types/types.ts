type TypePost = {
  authorId: string;
  authorName: string;
  authorUsername: string;
  authorProfile: string;
  postId: string;
  postTime: string;
  likes: string;
  textData: string;
  photoData: null | string;
};

export type { TypePost };
